#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 *puts_half - Prints half of a string
 *@str: input string to be printed
 */

void puts_half(char *str)
{
	int length;
	int startIndex;
	length = strlen(str);
	
	if (length % 2 ==0)
	{

	}



}
