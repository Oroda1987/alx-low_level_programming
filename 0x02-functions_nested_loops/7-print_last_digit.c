#include "main.h"
#include <stdio.h>
/**
 * print_last_digit - print the last digit of a number
 * @d: prameter to be checked
 * Return: Always 0
 */

int print_last_digit(int d)
{
	int n;
	int m;
	
	if(m = n % 10)
	{
		_putchar(m);
	}
	return (0);

}
	
