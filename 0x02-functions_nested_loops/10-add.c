#include "main.h"
#include <stdio.h>
/**
 * add -  add two numbers
 * @a: perameter to be checked
 * @b: perameter to be checked
 * Return: Always 0
 */

int add(int a, int b)
{
	int m;

	m  = add(a, b);
	_putchar(m);
	return (0);
}	
